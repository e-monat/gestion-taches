import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.santiagoemile.taches',
  appName: 'gestion-taches',
  webDir: 'dist'
};

export default config;
